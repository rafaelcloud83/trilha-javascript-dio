let variavel = 10 + 10;

console.log(variavel);

variavel = 10 - 10;

console.log(variavel);

variavel = 10 * 10;

console.log(variavel);

variavel = 10 / 10;

console.log(variavel);

// > maior que
// < menor que
// >= maior ou igual a
// <= menor ou igual a
// == igual a (ignora o tipo da variável)
// === igual a (considera o tipo da variável)
// != diferente de (ignora o tipo da variável)
// !== diferente de (considera o tipo da variável)
// && e lógico (and)
// || ou lógico (or)

let idade = 17;
if (idade >= 18) {
    console.log('Você é maior de idade');
} else {
    console.log('Você é menor de idade');
}